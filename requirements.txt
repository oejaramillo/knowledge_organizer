# ============================================================================
# Python dependencies for the three components:
#   zotero_sync/     — Zotero -> Postgres synchronisation
#   ai_enrichments/  — LLM enrichment of synced items
#   board/backend/   — FastAPI dashboard API
# Install with:  pip install -r requirements.txt
# ============================================================================

# ── Database ────────────────────────────────────────────────────────────────
# SQLAlchemy 1.4 is used by the dashboard API (board/backend/core/database.py).
SQLAlchemy==1.4.46
# psycopg2 is the driver SQLAlchemy 1.4 selects for a plain "postgresql://" URL.
psycopg2-binary==2.9.9
# psycopg 3 is used directly by zotero_sync/db.py and ai_enrichments/.
psycopg[binary]==3.1.18

# ── API / backend ───────────────────────────────────────────────────────────
fastapi==0.103.2
uvicorn==0.23.2
python-dotenv==1.0.0
requests==2.31.0
# The API schemas use the Pydantic v2 API (model_config / model_dump).
pydantic>=2.0,<3

# ── AI enrichment ───────────────────────────────────────────────────────────
# The OpenAI provider uses the v1 client API (from openai import OpenAI).
openai>=1.0,<2
pdfminer.six==20221105

# ── Intentionally not installed ─────────────────────────────────────────────
# tenacity — no retry decorators are used; zotero_sync/zotero_client.py
#            implements its own bounded backoff.
# pyzotero — replaced by the in-repo requests-based client
#            (zotero_sync/zotero_client.py).
